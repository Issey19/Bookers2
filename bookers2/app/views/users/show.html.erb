<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
            <table class="table">
            <tr>
              <th>name</th>
                <th><%= @user.name %></th>
            </tr>
                  <tr>
              <% if @user.id == current_user.id %>
              <th>introduction</th>
              <th></th>
                </tr>
              </table>
              <tr>
                <% if @user.id == current_user.id %>
                  <%= link_to "編集", edit_user_path(@user.id) >
                  <#class="btn btn-outline-secondary btn-block fas fa-user-cog" %>
                <% end %>
              <% end %>

        <div class="mt-3">
          <h2>New book</h2>
            <%= form_with model: @books, local:true do |f| %>

                  <h4>Title</h4>
                    <%= f.text_field :title %>
                    <h4>Opinion</h4>
                     <%= f.text_field :body %>
                  <br>
                  <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book">

            <% end %>
        </div>
      </div>


      <div class="col-md-8 offset-md-1">
            <h1>Books</h1>
              <table class="table table-hover table-inverse">
                <thaed><tr><td><td>Title</td><td><td>Opinion</td></tr></thaed>
                <tbody><% @book.each do |book| %>
                <tr><td><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg" %>
                <%= link_to @user.name  ,edit_user_path(@user) %></td>
                <td><%= book.title %></td>
                <td><%= book.body %></td>
                <% end %>
                </tbody>
                </tr>
              </table>
      </div>
    </div>
  </div>
</table>